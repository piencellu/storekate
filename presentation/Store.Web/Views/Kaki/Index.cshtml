@model Kaki

@{
    ViewData["Title"] = Model.Title;
}

<h1>@Model.Title</h1>
<p><strong>@Model.Authorkak</strong></p>
<p>@Model.Description</p>
<p>Цена вонючей какахи <strong>@Model.Price</strong> </p>
<form action="@Url.Action("Add","Cart")" method="post">
        <input type="hidden" name="id" value="@Model.Id"/>
        <button class="btn btn-outline-success" type="submit">В корзиночку</button>
</form>